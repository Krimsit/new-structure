{
  "name": "student-dashboard",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/student/dashboard/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project student-dashboard --web",
  "targets": {
    "dev-preview": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/student/dashboard",
        "commands": [
          "node dev"
        ]
      }
    }
  }
}
